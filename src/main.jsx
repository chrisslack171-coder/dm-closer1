import React from "react";
import { createRoot } from "react-dom/client";
import DMCloser from "./DMCloser.jsx";
createRoot(document.getElementById("root")).render(<React.StrictMode><DMCloser /></React.StrictMode>);
